name: Build CI - windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: make -gcc Ticket_Booking.c Registeration.c admin.c header.h unity_internals.h unity.h unit_testing.c unity.c 
