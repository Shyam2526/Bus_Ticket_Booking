name: Build CI on WINDOWS

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: Windows Latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: make -C 3_Implementation/version_2
